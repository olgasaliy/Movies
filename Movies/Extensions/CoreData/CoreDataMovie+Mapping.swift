//
//  CoreDataMovie.swift
//  Movies
//
//  Created by Olha Salii on 5/28/18.
//  Copyright © 2018 Olha Salii. All rights reserved.
//

/*import CoreData

extension CoreDataMovie {
    
    convenience init(movie: Movie, insertInto context: NSManagedObjectContext) {
        self.init(context: context)
        id = Int64(movie.id)
        title = movie.title
        releaseDate = movie.releaseDate
        posterPath = movie.posterPath /*saveImageToDisk(movie.posterPath)*/
        rating = movie.rating
        overview = movie.overview
        budget = movie.budget != nil ? Int64(movie.budget!) : 0
        genres = movie.genres
        originalLanguage = movie.originalLanguage
        isLiked = movie.isLiked
        
    }
    
    private func saveImageToDisk(_ image: String) -> String {
        //TODO: Save image to disk and return its path
        return ""
    }
}*/
